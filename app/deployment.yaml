apiVersion: v1
kind: Service
metadata:
  name: demosite
  labels:
    app: demosite
    service: demosite
spec:
  ports:
  - port: 3000
    name: http
  selector:
    app: demosite
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demosite
  labels:
    app: demosite
    version: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demosite
  template:
    metadata:
      labels:
        app: demosite
        version: green
    spec:
      containers:
      - name: demosite
        image: fabioandreola/demosite:green
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
